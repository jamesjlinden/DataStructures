#pragma once
#include <string>

template<typename IteratorType>
void PrintContainer(IteratorType begin, IteratorType end);

template<typename IteratorType>
std::string ContainerToString(IteratorType begin, IteratorType end);
